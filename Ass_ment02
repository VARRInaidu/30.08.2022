#include<stdio.h>
#include<string.h>
int main()
{
	char str[100];
	gets(str);
	int i,j,k;
	k=strlen(str);
	char ch;
	scanf("%c",&ch);
	for(i=0;i<k;i++)
	{
		if(str[i]==ch)
		{
			str[i]=str[i]-32;
		}
	}
	puts(str);
	return 0;
}
